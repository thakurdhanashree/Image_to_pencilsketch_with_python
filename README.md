# Image_to_pencilsketch_with_python
We need to read the image in RBG format and then convert it to a grayscale image, then invert the grayscale image, Then we can finally create the pencil sketch by mixing the grayscale image with the inverted blurry image.
